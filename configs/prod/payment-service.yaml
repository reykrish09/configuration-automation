application:
  name: payment-service
  environment: prod

server:
  port: 70000
  ssl_enabled: true

logging:
  level: WARN

database:
  host: db-prod.internal
  port: 5432

performance:
  max_connections: 500
